# Movie-Collection-Manager-App
A simple Python command-line Movie Manager app to add, view, search, update, and delete movies. Uses classes and data structures to store movie details with unique IDs and input validation. Provides a text-based menu and works fully offline with no database.
